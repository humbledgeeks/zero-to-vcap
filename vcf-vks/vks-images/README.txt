Drop ALL screenshots for the VKS / Doom post here.

Naming: any order is fine on the way in — I'll rename into workflow order.
Suggested workflow order (we'll finalize when all are in):
  00x  Supervisor health / NSX VPC networking
  01x  vSphere Namespace create (vks-doom)
  02x  Storage policy add (supervisor)
  03x  VM classes add (best-effort-medium + large)
  04x  Content library (Kubernetes Service Content Library)
  05x  CLI tools download + vcf context login
  06x  kubectl get ns / discovery
  07x  Cluster apply + machines provisioning
  08x  Guest kubeconfig + Kubedoom + the Doom screen
